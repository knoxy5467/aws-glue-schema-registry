# Phase 4.9 — Java Parity Audit

## Front matter

- Date: 2026-06-23
- Branch: phase-4.9-audit
- Base commit: 0a95eae (golang-mrknox)
- Go module root: native-schema-registry/golang/
- Auditor: swarm-implementor
- Code-review status: `/code-review` plugin contraindicated by AC10 (PR required); substituted cumulative multi-perspective review per IMPL_RESULT; findings applied in 93222a7 (and 623654c follow-up); 2 NIT findings deferred — see §4.

## Summary counters

| Category | Implemented | Stubbed | Missing | Total |
|---|---:|---:|---:|---:|
| Java parity (config + client + facades) | 17 | 17 | 5 | 39 |
| Format-layer Phase 3 stubs (3 rows) | 2 | 1 | 0 | 3 |
| §5.3 test coverage (special-attention items) | Tier-1: 12 Y / 5 N · Tier-2: 17 Y / 0 N | — | Tier-1∧Tier-2 N: 0 | 17 |

## TL;DR

17 implemented / 17 stubbed / 5 missing across 39 Java surfaces; format layer 2/3 done; 1 multi-phase carry-over (JSON-Schema migration); see §4 for deferred findings.

## 1. Java parity audit

### 1.1 GlueSchemaRegistryConfiguration

Java source: `common/src/main/java/com/amazonaws/services/schemaregistry/common/configs/GlueSchemaRegistryConfiguration.java` (353 lines) at `0a95eae`.

Go counterpart: `native-schema-registry/golang/pkg/gsrserde-go/core/config.go` (+ `config_test.go`) at HEAD.

| Java file:line | Java behavior | Go file:line \| Missing | Status | Notes |
|---|---|---|---|---|
| `common/.../GlueSchemaRegistryConfiguration.java:46` (field), `:133-149` (validateAndSetCompressionType) | `compression` config key → `compressionType` enum (`NONE` or `ZLIB`); default `NONE`; throws `AWSSchemaRegistryException` on invalid values. | `pkg/gsrserde-go/core/config.go:29` (constant), `:153-161` (parse), `:191` (assignment) | Implemented | Go also accepts legacy alias key `compressionType`; Java key wins when both are set. |
| `common/.../GlueSchemaRegistryConfiguration.java:47` (field), `:193-197` (validateAndSetAWSEndpoint) | `endpoint` config key → optional Glue endpoint URL string; no default; passed through verbatim. | `pkg/gsrserde-go/core/config.go:23` (constant), `:141` (read), `:185` (assignment) | Implemented | |
| `common/.../GlueSchemaRegistryConfiguration.java:48` (field), `:151-165` (validateAndSetAWSRegion) | `region` config key → AWS region string; falls back to `DefaultAwsRegionProviderChain` when absent; throws when neither configured nor discoverable. | `pkg/gsrserde-go/core/config.go:22` (constant), `:97` (read), `:103-105` (load option), `:184` (assignment) | Implemented | untested — no Tier-1 test asserts the SDK-default fallback path; only the explicit-region path is covered by `config_test.go:42-48`. |
| `common/.../GlueSchemaRegistryConfiguration.java:49` (field), `:234-246` (validateAndSetCacheTTL) | `timeToLiveMillis` config key → cache TTL in ms; default 86400000 (24h); throws on non-numeric input. | `pkg/gsrserde-go/core/config.go:30` (constant), `:47` (default), `:168-173` (parse + assignment) | Stubbed | Go silently falls back to the default on non-numeric input rather than throwing — `strconv.ParseInt` errors are swallowed at `config.go:170-172`. |
| `common/.../GlueSchemaRegistryConfiguration.java:50` (field), `:219-232` (validateAndSetCacheSize) | `cacheSize` config key → max cache entries; default 200; throws on non-numeric input. | `pkg/gsrserde-go/core/config.go:31` (constant), `:48` (default), `:175-180` (parse + assignment) | Stubbed | Go silently falls back to the default on non-numeric input rather than throwing — `strconv.Atoi` errors are swallowed at `config.go:177-179`. |
| `common/.../GlueSchemaRegistryConfiguration.java:51` (field), `:248-253` (validateAndSetAvroRecordType) | `avroRecordType` config key → Avro record-type enum (`SPECIFIC_RECORD` or `GENERIC_RECORD`); no default; throws on invalid enum. | `pkg/gsrserde-go/core/config.go:32` (constant), `:195` (assignment) | Stubbed | Go parses the string into `Config.AvroRecordType` but the Avro deserializer does not consume it — type selection is fixed to GenericRecord-equivalent. No enum validation; arbitrary strings are accepted. |
| `common/.../GlueSchemaRegistryConfiguration.java:52` (field), `:255-260` (validateAndSetProtobufMessageType) | `protobufMessageType` config key → Protobuf message-type enum (`POJO` or `DYNAMIC_MESSAGE`); no default; throws on invalid enum. | `pkg/gsrserde-go/core/config.go:33` (constant), `:196` (assignment) | Stubbed | Go parses the string into `Config.ProtobufMessageType` but the protobuf deserializer does not consume it — message-type selection happens via Go reflection rather than this key. No enum validation. |
| `common/.../GlueSchemaRegistryConfiguration.java:53` (field), `:185-191` (validateAndSetRegistryName) | `registry.name` config key → Glue registry name; default `"default-registry"`. | `pkg/gsrserde-go/core/config.go:25` (constant), `:44` (default), `:143-146` (read + default), `:187` (assignment) | Implemented | |
| `common/.../GlueSchemaRegistryConfiguration.java:54` (field), `:167-183` (validateAndSetCompatibility) | `compatibility` config key → Glue `Compatibility` enum; default `BACKWARD`; throws on unknown enum. | `pkg/gsrserde-go/core/config.go:26` (constant), `:45` (default), `:148-151` (read + default), `:188` (assignment) | Stubbed | Go stores the value as a string and forwards it to the SDK's `types.Compatibility(...)` cast in `encoder.go:351`. No enum validation; arbitrary strings are accepted and only fail at the Glue API call. |
| `common/.../GlueSchemaRegistryConfiguration.java:55` (field), `:211-217` (validateAndSetDescription) | `description` config key → schema description; default is auto-built from `DEFAULT-DESCRIPTION-<region>-<registryName>` (line 343-352). | `pkg/gsrserde-go/core/config.go:27` (constant), `:189` (assignment) | Stubbed | Go does not compute the `DEFAULT-DESCRIPTION-<region>-<registryName>` default; empty input stays empty. Description is then forwarded to `CreateSchema` in `encoder.go:352` even when blank. |
| `common/.../GlueSchemaRegistryConfiguration.java:56` (field), `:262-271` (validateAndSetSchemaAutoRegistrationSetting) | `schemaAutoRegistrationEnabled` config key → boolean; default `false`; parsed via `Boolean.parseBoolean` (only `"true"` is true). | `pkg/gsrserde-go/core/config.go:28` (constant), `:163-166` (parse), `:190` (assignment), `:210-212` (`parseBool`) | Implemented | |
| `common/.../GlueSchemaRegistryConfiguration.java:57` (field), `:273-285` (validateAndSetTags) | `tags` config key → `Map<String,String>` of resource tags propagated to `CreateSchema`; throws when not a `HashMap`. | `pkg/gsrserde-go/core/config.go:34` (constant), `:203` (`collectPrefixedMap`), `:216-224` (impl) | Implemented | Go diverges: instead of a single `Map<String,String>` key, callers pass `tags.<key>=<value>` flat-map entries which Go expands into `Config.Tags`. Same external behavior — values flow into `CreateSchema.Tags` (`encoder.go:353`). |
| `common/.../GlueSchemaRegistryConfiguration.java:58` (field), `:287-296` (validateAndSetMetadata) | `metadata` config key → `Map<String,String>` of schema-version metadata propagated into `PutSchemaVersionMetadata`; throws when not a `HashMap`. | `pkg/gsrserde-go/core/config.go:35` (constant), `:204` (`collectPrefixedMap`), `:216-224` (impl) | Stubbed | Go parses `metadata.<key>=<value>` into `Config.Metadata` but the field is never consumed — `CreateSchema` and `RegisterSchemaVersion` call sites in `encoder.go` ignore it, and there is no Go call site for `PutSchemaVersionMetadata`. |
| `common/.../GlueSchemaRegistryConfiguration.java:59` (field), `:109-120` (validateAndSetSecondaryDeserializer) | `secondaryDeserializer` config key → fully-qualified class name for fallback deserializer; accepts `String` or `Class`; throws on other types. | `pkg/gsrserde-go/core/config.go:36` (constant), `:197` (assignment) | Stubbed | Go parses the value into `Config.SecondaryDeserializer` but no Go decoder fallback path consumes it; no secondary-deserializer chain exists. |
| `common/.../GlueSchemaRegistryConfiguration.java:60` (field), `:199-209` (validateAndSetProxyUrl) | `proxyUrl` config key → `URI` parsed and installed onto the HTTP client; throws on invalid URI. | `pkg/gsrserde-go/core/config.go:24` (constant), `:112-119` (parse + install on transport), `:186` (assignment) | Implemented | |
| `common/.../GlueSchemaRegistryConfiguration.java:66` (field, default `"default"`), `:127-131` (validateAndSetUserAgent) | `userAgentApp` config key → application name appended to AWS User-Agent; default `"default"`. | `pkg/gsrserde-go/core/config.go:37` (constant), `:106-111` (install middleware), `:198` (assignment) | Stubbed | Go does NOT apply the `"default"` default — when the key is absent `Config.UserAgentApp` stays empty and the `glue-schema-registry-go/<ua>` middleware key is not added (only added when the user explicitly sets the key). |
| `common/.../GlueSchemaRegistryConfiguration.java:68` (field), `:298-310` (validateAndSetJacksonSerializationFeatures) | `jacksonSerializationFeatures` config key → `List<SerializationFeature>` for Jackson JSON encoder; no default; throws when not a `List`. | Missing | Missing | Java-only — Go uses `encoding/json` and the JSON Schema validator (`xeipuuv/gojsonschema`); there is no Jackson surface to forward feature flags to. |
| `common/.../GlueSchemaRegistryConfiguration.java:69` (field), `:312-324` (validateAndSetJacksonDeserializationFeatures) | `jacksonDeserializationFeatures` config key → `List<DeserializationFeature>` for Jackson JSON decoder; no default; throws when not a `List`. | Missing | Missing | Same divergence as `jacksonSerializationFeatures` — Go has no Jackson layer; no Go-side equivalent surface exists. |

### 1.2 AWSSchemaRegistryClient

Java source: `common/src/main/java/com/amazonaws/services/schemaregistry/common/AWSSchemaRegistryClient.java` (572 lines) at `0a95eae`.

Go counterpart: `native-schema-registry/golang/pkg/gsrserde-go/core/client_interface.go` (interface) + call sites in `encoder.go` / `decoder.go` at HEAD.

| Java file:line | Java behavior | Go file:line \| Missing | Status | Notes |
|---|---|---|---|---|
| `common/.../AWSSchemaRegistryClient.java:85-117`, `:126-129`, `:131-133` | Public constructors — wire `GlueClient` with credentials, retry policy, user-agent interceptor, proxy, and optional endpoint override. | `pkg/gsrserde-go/core/config.go:96-206` (`LoadConfigFromMap` builds `aws.Config`), `pkg/gsrserde-go/core/encoder.go:61-84` (`NewGsrEncoder`), `pkg/gsrserde-go/core/decoder.go:30-48` (`NewGsrDecoder`) | Implemented | Go diverges: the Glue SDK client is instantiated inside `NewGsrEncoder`/`NewGsrDecoder` from `aws.Config` rather than via a dedicated `AWSSchemaRegistryClient` constructor, but the same wiring (region, endpoint, proxy, user-agent middleware, assume-role) is applied at config-load time. |
| `common/.../AWSSchemaRegistryClient.java:143-157` (`getSchemaVersionIdByDefinition`) | Invokes Glue `GetSchemaByDefinition` to resolve a schema definition + name to a `UUID`; wraps any failure in `AWSSchemaRegistryException`. | `pkg/gsrserde-go/core/client_interface.go:29` (interface), `pkg/gsrserde-go/core/encoder.go:210-216` (call site in `fetchSchemaVersionID`) | Implemented | |
| `common/.../AWSSchemaRegistryClient.java:168-181` (`getSchemaVersionResponse`) | Invokes Glue `GetSchemaVersion` by `schemaVersionId`; validates the response is non-null and carries the same id; wraps failures in `AWSSchemaRegistryException`. | `pkg/gsrserde-go/core/client_interface.go:30` (interface), `pkg/gsrserde-go/core/decoder.go:151-153` (call site in `getSchemaByVersionID`) | Implemented | |
| `common/.../AWSSchemaRegistryClient.java:242-267` (`createSchema`) | Invokes Glue `CreateSchema` with `dataFormat`, `description`, `registryId`, `schemaName`, `schemaDefinition`, `compatibility`, `tags`; on `AlreadyExistsException` falls back to `registerSchemaVersion`; then calls `putSchemaVersionMetadata` to attach metadata. | `pkg/gsrserde-go/core/client_interface.go:31` (interface), `pkg/gsrserde-go/core/encoder.go:337-370` (`createSchema`), `:272-279` (`AlreadyExistsException` → `registerSchemaVersion` fallback) | Stubbed | The `putSchemaVersionMetadata` follow-up call is omitted — `Config.Metadata` never reaches the Glue side. Tags and description ARE propagated; only metadata is dropped. |
| `common/.../AWSSchemaRegistryClient.java:278-321` (`registerSchemaVersion`) | Invokes Glue `RegisterSchemaVersion`; if `statusAsString == AVAILABLE` returns immediately, otherwise polls via `waitForSchemaEvolutionCheckToComplete` (up to 10 retries with a 3s pause); on the metadata-overload also fires `putSchemaVersionMetadata`. | `pkg/gsrserde-go/core/client_interface.go:32` (interface), `pkg/gsrserde-go/core/encoder.go:295-327` (`registerSchemaVersion`) | Stubbed | Go does NOT poll for `PENDING → AVAILABLE` — it returns the raw response immediately and assumes `AVAILABLE`. There is no equivalent of `waitForSchemaEvolutionCheckToComplete`, no `MAX_ATTEMPTS=10`, no `MAX_WAIT_INTERVAL=3000ms`. The `putSchemaVersionMetadata` follow-up is also omitted. |
| `common/.../AWSSchemaRegistryClient.java:418-429` (`putSchemaVersionMetadata(UUID, Map)` overload) and `:438-451` (`putSchemaVersionMetadata(UUID, MetadataKeyValuePair)` overload) | Invokes Glue `PutSchemaVersionMetadata` in parallel for each `Map.Entry` of metadata; logs warnings on individual failures without throwing. | `pkg/gsrserde-go/core/client_interface.go:33` (interface declaration only) | Stubbed | The interface method is declared but never invoked from any non-test Go file (verified via `grep -rn "PutSchemaVersionMetadata" pkg/gsrserde-go/ --include="*.go"` finds only `client_interface.go` and test mocks). |
| `common/.../AWSSchemaRegistryClient.java:476-487` (`querySchemaVersionMetadata`) | Invokes Glue `QuerySchemaVersionMetadata` by `schemaVersionId`; wraps failures in `AWSSchemaRegistryException`. | `pkg/gsrserde-go/core/client_interface.go:34` (interface declaration only) | Stubbed | The interface method is declared but never invoked from any non-test Go file. |
| `common/.../AWSSchemaRegistryClient.java:502-517` (`querySchemaTags`) | Invokes Glue `GetSchemaByDefinition` to resolve schema ARN, then Glue `GetTags` against that ARN; wraps failures in `AWSSchemaRegistryException`. | `pkg/gsrserde-go/core/client_interface.go:35` (interface declaration only) | Stubbed | The interface method is declared but never invoked from any non-test Go file; there is no Go-side equivalent of `querySchemaTags`. |

### 1.3 GlueSchemaRegistrySerializationFacade

Java source: `serializer-deserializer/src/main/java/com/amazonaws/services/schemaregistry/serializers/GlueSchemaRegistrySerializationFacade.java` (143 lines) at `0a95eae`.

Go counterpart: `native-schema-registry/golang/pkg/gsrserde-go/serializer/gsr_serializer.go` at HEAD.

| Java file:line | Java behavior | Go file:line \| Missing | Status | Notes |
|---|---|---|---|---|
| `serializer-deserializer/.../GlueSchemaRegistrySerializationFacade.java:48-74` (`@Builder` constructor) | Builds the facade from a credentials provider plus one of `glueSchemaRegistryConfiguration`, `configs` map, or `Properties`; throws when none are provided; lazily constructs a `SchemaByDefinitionFetcher` over an `AWSSchemaRegistryClient`; instantiates `SerializationDataEncoder`. | `pkg/gsrserde-go/serializer/gsr_serializer.go:40-49` (`NewSerializer`), `:54-56` (`NewSerializerWithEncoder`), `:61-96` (`NewSerializerWithEncoderAndStrategy`) | Implemented | Go diverges: instead of `Properties`/`configMap`/`config` triple-overload it takes a single `*common.Configuration`, and instead of a `@Builder` it exposes three explicit constructors including a test-seam variant that accepts a pre-built `*gsrcore.GsrEncoder`. Same external behavior — facade wraps an encoder + format serializer. |
| `serializer-deserializer/.../GlueSchemaRegistrySerializationFacade.java:76-89` (`getOrRegisterSchemaVersion`) | Resolves a schema name + definition to a `UUID` by delegating to `SchemaByDefinitionFetcher.getORRegisterSchemaVersionId`; injects `TRANSPORT_METADATA_KEY = "x-amz-meta-transport"` into the metadata map plus any user-configured metadata. | `pkg/gsrserde-go/core/encoder.go:166-201` (`getSchemaVersionIdByDefinition`), `:207-293` (`fetchSchemaVersionID`) | Stubbed | The Glue resolution-or-register flow lives in core's encoder rather than as a facade method, and the `x-amz-meta-transport` transport metadata key is NOT injected — `transportName` is accepted by `Encode` but only used for cache-key formatting, not propagated as a metadata-key value. |
| `serializer-deserializer/.../GlueSchemaRegistrySerializationFacade.java:102-110` (`serialize(DataFormat, Object, UUID)`) | Picks a format-specific `GlueSchemaRegistryDataFormatSerializer` via `glueSchemaRegistrySerializerFactory.getInstance(dataFormat, ...)`; calls `dataFormatSerializer.serialize(data)`; prepends the GSR wire-format prefix via `serializationDataEncoder.write(serializedBytes, schemaVersionId)`. | `pkg/gsrserde-go/serializer/gsr_serializer.go:100-131` (`Serialize`) | Implemented | Go diverges: the `Serialize(topic, data)` Go signature corresponds to the public Kafka-customer surface, not the lower-level `(DataFormat, Object, UUID)` Java overload. Same external pipeline — format-layer encode → wire-format wrap via `coreEncoder.Encode`. |
| `serializer-deserializer/.../GlueSchemaRegistrySerializationFacade.java:112-133` (`encode(String, Schema, byte[])`) | Pre-validated public encode entry: looks up format serializer, calls `dataFormatSerializer.validate(schemaDefinition, data)` (no-op for Avro, JSON Schema validation for JSON), resolves `schemaVersionId` via `getOrRegisterSchemaVersion`, and wraps the bytes with `serializationDataEncoder.write`. | Missing | Missing | The Go `Serializer.Serialize` accepts an `interface{}` (typed object) rather than already-serialized bytes; there is no public Go path equivalent to `encode(topic, Schema, byte[])` for callers who already hold serialized bytes and a `Schema`. |
| `serializer-deserializer/.../GlueSchemaRegistrySerializationFacade.java:135-141` (`getSchemaDefinition(DataFormat, Object)`) | Picks the format-specific serializer and asks it to extract the schema definition string from the live object (e.g. `.toString()` of Avro `Schema`, json-schema string from JSR-303 helper, etc.). | `pkg/gsrserde-go/serializer/gsr_serializer.go:165-168` (`schemaFromData` → `formatSerializer.GetSchemaDefinition(data)`) | Stubbed | The Go equivalent lives inside `schemaFromData` (a private helper) and is not exposed as a public method on `Serializer`. External callers cannot ask for the schema definition without performing a full encode. |

### 1.4 GlueSchemaRegistryDeserializationFacade

Java source: `serializer-deserializer/src/main/java/com/amazonaws/services/schemaregistry/deserializers/GlueSchemaRegistryDeserializationFacade.java` (261 lines) at `0a95eae`.

Go counterpart: `native-schema-registry/golang/pkg/gsrserde-go/deserializer/gsr_deserializer.go` at HEAD.

| Java file:line | Java behavior | Go file:line \| Missing | Status | Notes |
|---|---|---|---|---|
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:73-92` (`@Builder` constructor), `:94-100` (`initializeCache`), `:102-108` (alternate constructor) | Builds the facade from one of `configs`/`properties` plus a credentials provider and optional `AWSSchemaRegistryClient`; instantiates the Guava `LoadingCache<UUID, Schema>` keyed by schema-version id with `maximumSize(cacheSize)` and `refreshAfterWrite(timeToLiveMillis)`. | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:31-40` (`NewDeserializer`), `:45-63` (`NewDeserializerWithDecoder`), `pkg/gsrserde-go/core/decoder.go:30-48` (`NewGsrDecoder` + cache wiring) | Implemented | Go diverges: a single `*common.Configuration` replaces the `configs`/`properties` overload; the cache is owned by `*gsrcore.GsrDecoder` rather than the facade. Same caching behavior (max-size + TTL) is established via `gsrcore.NewCache` (`core/decoder.go:38`). |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:115-117` (`overrideUserAgentApp`) | Mutates `glueSchemaRegistryConfiguration.userAgentApp` post-construction; takes effect on subsequent Glue calls' User-Agent header. | Missing | Missing | No Go-side equivalent — `Deserializer` does not expose a user-agent override; `Config.UserAgentApp` is set at construction time only. |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:127-131` (`getSchemaDefinition(ByteBuffer)`) and `:153-155` (`getSchemaDefinition(byte[])`) | Parses the GSR wire-format prefix from the input, looks up the `Schema` from cache (with Glue `GetSchemaVersion` fallback via the loading-cache loader), and returns `schema.schemaDefinition`. | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:116-124` (`GetSchema` returns the whole `*gsrcore.Schema`), `pkg/gsrserde-go/core/decoder.go:108-114` (`DecodeSchema`) | Stubbed | Go returns the full `*Schema` object via `GetSchema`; callers must read `.SchemaDefinition` themselves. There is no public `GetSchemaDefinition(byte[]) (string, error)` shorthand on the facade. |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:133-136` (`getActualData`) | Strips the 18-byte GSR wire-format prefix from the payload and returns the underlying plain bytes; uses `GlueSchemaRegistryDeserializerDataParser.getPlainData`. | Missing | Missing | No public `Deserializer.GetActualData(data []byte) ([]byte, error)` method exists. The plain-bytes extraction happens internally inside `core/decoder.go:53-83` (`Decode`) but is coupled to the full decode flow; callers cannot reach the raw post-prefix bytes without performing a format-layer decode. |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:138-143` (`getSchema(byte[])`) | Same as `getSchemaDefinition` but returns the full `Schema` (definition + data-format + schema-name). | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:116-124` (`GetSchema`) | Implemented | |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:165-175` (`deserialize(AWSDeserializerInput)`) | Parses the wire-format prefix, looks up the writer schema from cache (or fetches via Glue), picks a format-specific `GlueSchemaRegistryDataFormatDeserializer`, and decodes the payload into a typed object. | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:70-101` (`Deserialize`) | Implemented | Go's `Deserialize(topic, data)` accepts a `topic` for parity with the public Kafka-customer surface even though core ignores it (the wire-format prefix already carries the schema-version-id). |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:182-188` (`canDeserialize`) | Returns true iff the byte array is GSR-framed (passes `GlueSchemaRegistryDeserializerDataParser.isDataCompatible`); returns false on null input. | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:104-112` (`CanDeserialize`), `pkg/gsrserde-go/core/decoder.go:86-104` (`CanDecode` / `CanDecodeData`) | Implemented | |
| `serializer-deserializer/.../GlueSchemaRegistryDeserializationFacade.java:239-240` (`close`) | Empty close — `Closeable` interface compliance only; no resources released. | `pkg/gsrserde-go/deserializer/gsr_deserializer.go:132-143` (`Close`) | Implemented | Go's `Close` is non-empty — it closes the underlying cache. External contract (idempotent, safe-after-close) is the same. |

## 2. Format-layer stub audit

| Component | Phase 3 expected outcome | Current state (file:line evidence) | Verdict |
|---|---|---|---|
| `protobuf.ProtobufSerializer.Validate` — `native-schema-registry/golang/pkg/gsrserde-go/serializer/protobuf/protobuf_serializer.go:242` | Replace the null-check-only stub with real proto-wire validation (walk bytes via `protowire`/`proto.Unmarshal` to reject non-proto payloads). | At `protobuf_serializer.go:263`: `_, _, n := protowire.ConsumeField(data[i:])` — the body walks the input field-by-field; `protobuf_serializer.go:264-266` reacts to a negative consumer length by returning a typed `*ProtobufValidationError` whose cause is `protowire.ParseError(n)` chained through `ErrValidation`. | Done |
| `protobuf.ProtobufDeserializer.NewProtobufDeserializer` — `native-schema-registry/golang/pkg/gsrserde-go/deserializer/protobuf/protobuf_deserializer.go:61` | Replace the panic-on-nil-descriptor stub with a typed error return so callers can recover. | At `protobuf_deserializer.go:66`: `return nil, ErrNilDescriptor` — the constructor now returns a typed sentinel (chained through `core.ErrInvalidProtobufPayload`) when the descriptor is nil; line `:63` returns `common.ErrNilConfig` when config itself is nil. No `panic(` call remains in this constructor body (lines 61-72). | Done |
| `JSON-Schema library` choice — referenced from `native-schema-registry/golang/go.mod:12`, `native-schema-registry/golang/pkg/gsrserde-go/serializer/json/json_serializer.go:8`, `native-schema-registry/golang/pkg/gsrserde-go/deserializer/json/json_deserializer.go:9` | Migrate the JSON-Schema validator from `github.com/xeipuuv/gojsonschema` (Draft-04/06/07) to `github.com/santhosh-tekuri/jsonschema/v6` (Draft 2020-12). | At `go.mod:12`: `github.com/xeipuuv/gojsonschema v1.2.0` is the direct dependency declared by the module. At `json_serializer.go:8` and `json_deserializer.go:9`: `"github.com/xeipuuv/gojsonschema"` is the import path used by the JSON validate path. `grep -RIn 'xeipuuv/gojsonschema\|santhosh-tekuri/jsonschema' native-schema-registry/golang/` returns no `santhosh-tekuri/jsonschema` reference in the main Go module (the only `santhosh-tekuri/jsonschema/v5` hit lives in `integration-tests/go.sum:264` as an indirect transitive). | Still stub |


## 3. §5.3 test-coverage audit

Tier-1 = unit tests under `native-schema-registry/golang/pkg/gsrserde-go/.../*_test.go`. Tier-2 = integration tests under `native-schema-registry/golang/integration-tests/tests/*_test.go`. All file paths in citations are relative to `native-schema-registry/golang/`. Every `Y` cell carries `file:TestFuncName:line`. Cell line numbers point to the `func Test...` declaration line. Cited tests verified by `grep -n '^func Test' <file>` at HEAD.

Tier-2 Y means a test file exists at the cited file:line that targets this scenario; it does NOT guarantee the test runs and passes — see §4 for tests currently `t.Skip`-ed.

| Item # | Item description | Tier-1 covered (Y/N + file:TestFuncName:line) | Tier-2 covered (Y/N + file:TestFuncName:line) | Notes |
|---|---|---|---|---|
| 16 | Cache TTL eviction: after TTL, a fresh `GetSchemaVersion` call is made. | Y, `pkg/gsrserde-go/core/cache_test.go:TestCacheTTL:8` | Y, `integration-tests/tests/schema_lifecycle_test.go:TestLifecycle_CacheTTLEviction:159` | Tier-1 covers the bare cache TTL-eviction behavior on the `Cache` interface; Tier-2 covers TTL eviction observed via the encoder's GetSchemaByDefinition recount on the fakeglue backend. |
| 17 | Cache size eviction: distinct schemas beyond `cacheSize` evict the oldest. | Y, `pkg/gsrserde-go/core/cache_test.go:TestCacheSize:60` (stand-in; size-eviction contract not asserted) | Y, `integration-tests/tests/schema_lifecycle_test.go:TestLifecycle_CacheSizeEviction:205` | Tier-1 test is a basic-functionality stand-in (the file's own comment notes `go-cache doesn't have built-in size limits`), so the size-eviction contract is not actually asserted. Tier-2 test is `t.Skip`-ed pending a cache size cap. |
| 18 | BACKWARD evolution v1→v2: producer publishes v1, consumer with v2 reads successfully. | N | Y, `integration-tests/tests/compatibility_test.go:TestCompatibility_BackwardV1ToV2:108` | Tier-2 covers wire-flow under the BACKWARD setting; Tier-1 has no compat-evolution test. |
| 19 | BACKWARD_ALL across three versions. | N | Y, `integration-tests/tests/compatibility_test.go:TestCompatibility_BackwardAll_ThreeVersions:126` | Tier-2 only; `requiresFake=true` since the assertion uses fakeglue `Snapshot()`. |
| 20 | FORWARD evolution v2→v1. | N | Y, `integration-tests/tests/compatibility_test.go:TestCompatibility_ForwardV2ToV1:153` | Tier-2 only; backend-agnostic per the test's own docstring. |
| 21 | FULL evolution both directions. | N | Y, `integration-tests/tests/compatibility_test.go:TestCompatibility_FullBothDirections:169` | Tier-2 only; `requiresFake=true` for the Snapshot()-based total-count assertion. |
| 22 | Incompatible schema change rejected at `CreateSchema` time when compatibility is set. | Y, `pkg/gsrserde-go/core/glue_negatives_test.go:TestEncoder_CompatibilityRejection_SurfacesTypedError:189` | Y, `integration-tests/tests/compatibility_test.go:TestCompatibility_IncompatibleRejected:190` | Tier-2 also has `TestCompatibility_IncompatibleRejected_Real:225` for `requiresReal=true` server-side enforcement. |
| 23 | IAM denied → returns a typed error wrapping the SDK's `AccessDeniedException`. | Y, `pkg/gsrserde-go/core/glue_negatives_test.go:TestEncoder_IAMDenied_PreservesTypedSDKError:110` | Y, `integration-tests/tests/negative_test.go:TestNegative_IAMDenied:54` | Both tiers assert the SDK error type is preserved through the encoder's wrapping chain. |
| 24 | Throttling (Glue `ThrottlingException`) → retried per the SDK default retryer; surfaces only after exhausted retries. | Y, `pkg/gsrserde-go/core/glue_negatives_test.go:TestEncoder_Throttling_SurfacesAsErrGSRChain:88` | Y, `integration-tests/tests/negative_test.go:TestNegative_Throttling:89` | Tier-1 covers the chain-to-ErrGSR contract; Tier-2 drives the throttling path via fakeglue's force-error mechanism. |
| 25 | `EntityNotFoundException` on `GetSchemaVersion` + `schemaAutoRegistrationEnabled=true` → falls through to `CreateSchema` / `RegisterSchemaVersion`. | Y, `pkg/gsrserde-go/core/glue_negatives_test.go:TestEncoder_EntityNotFound_AutoRegisterTrue_FallsThroughToCreateSchema:137` | Y, `integration-tests/tests/negative_test.go:TestNegative_EntityNotFoundFallsThroughToCreate:124` | Tier-1 also covers the auto-register=false sentinel path at `TestEncoder_EntityNotFound_AutoRegisterFalse_SurfacesSentinel:166`. |
| 26 | Malformed JSON, malformed Avro, malformed Protobuf — each returns a sentinel error. | Y, `pkg/gsrserde-go/deserializer/avro/avro_malformed_test.go:TestAvroDeserializer_MalformedBytes_SurfacesTypedError:25` | Y, `integration-tests/tests/negative_test.go:TestNegative_MalformedDecodePayload:164` | Tier-1 split across format packages: Avro at `avro_malformed_test.go:TestAvroDeserializer_MalformedBytes_SurfacesTypedError:25`; JSON at `pkg/gsrserde-go/deserializer/json/json_malformed_test.go:TestJsonDeserializer_NonUtf8_SurfacesMalformedJsonError:25`; Protobuf at `pkg/gsrserde-go/deserializer/protobuf/protobuf_deserializer_test.go:TestProtobufDeserializer_Deserialize_ErrorCases:182` (`invalid protobuf data` table case surfaces `ErrDeserializationFailed`). |
| 27 | UTF-8 vs non-UTF-8 JSON payload — non-UTF-8 returns "Malformed JSON" sentinel. | Y, `pkg/gsrserde-go/deserializer/json/json_malformed_test.go:TestJsonDeserializer_NonUtf8_SurfacesMalformedJsonError:25` | Y, `integration-tests/tests/negative_test.go:TestNegative_NonUTF8Path:217` | Tier-1 asserts the typed `*JsonDeserializationError`; Tier-2 drives the path end-to-end. |
| 28 | Truncated payload (less than 18 bytes) → typed error. | Y, `pkg/gsrserde-go/core/payload_negatives_test.go:TestGsrDecoder_TruncatedPayload_SurfacesIncompatibleData:32` | Y, `integration-tests/tests/negative_test.go:TestNegative_TruncatedPayload:268` | Both tiers assert the chain reaches `ErrIncompatibleData` / `ErrGSR`. |
| 29 | Header version byte `0x03` but corrupt UUID → typed error. | Y, `pkg/gsrserde-go/core/payload_negatives_test.go:TestGsrDecoder_ValidHeader_UnknownUUID_SurfacesTypedError:60` | Y, `integration-tests/tests/negative_test.go:TestNegative_UnknownVersionUUID:299` | Per the Tier-1 docstring, "valid header, unknown UUID" and "valid header, corrupted UUID" hit the same wire-format code path; coverage of one is coverage of the other. |
| 30 | Multithreaded produce/consume with shared serializer/deserializer instances. | N | Y, `integration-tests/tests/concurrency_test.go:TestConcurrency_SharedInstancesDoNotRace:33` | Tier-2 also has `multithreaded_integration_suite_test.go:TestMultiThreadedIntegrationSuite:531` running the broader sarama-based multithreaded suite. |
| 31 | Concurrent first-encode of the same schema only calls `CreateSchema` once (singleflight-style assertion on the mock). | Y, `pkg/gsrserde-go/core/singleflight_test.go:TestEncoder_GetSchemaVersionId_SingleflightDeduplicates:24` | Y, `integration-tests/tests/concurrency_test.go:TestConcurrency_SingleflightFirstEncode:118` | Tier-1 also has `TestDecoder_GetSchema_SingleflightDeduplicates:113` for the decoder side. |
| 32 | Every recognized config key is honored — separate small test per key verifying the side-effect. | Y, `pkg/gsrserde-go/core/config_test.go:TestConfigKeys_AreJavaIdentical:236` | Y, `integration-tests/tests/config_keys_test.go:TestConfig_RecognizedKeysFlowThrough:37` | Tier-1 has per-key tests across `config_test.go:14-235` (region, endpoint, proxyURL, registry.name, compatibility, description, schemaAutoRegistrationEnabled, compressionType, timeToLiveMillis, cacheSize, avroRecordType, protobufMessageType, secondaryDeserializer, userAgentApp, assumeRoleArn, schemaNameGenerationClass, tags, metadata). |

## 4. Risks / Deferred findings

- **Audit-meta:** §3 Item 17 (Cache size eviction) is marked Tier-1 `Y` but the Tier-1 test (`pkg/gsrserde-go/core/cache_test.go:TestCacheSize:60`) is a basic-functionality stand-in — its own in-file comment notes `go-cache doesn't have built-in size limits`, so the size-eviction contract is not actually asserted. The Tier-2 counterpart (`integration-tests/tests/schema_lifecycle_test.go:TestLifecycle_CacheSizeEviction:205`) is `t.Skip`-ed pending a cache size cap. Net effect: row counted as covered in §3 by the row-presence rule, but behavioral coverage of the size-eviction contract is absent. Surfaced for Phase 4.10 reassessment, not remediated here. (Origin: PBI-3 row authoring; preserved through PBI-4 audit closeout.)
- **Audit-meta (Path A consequence):** Several §1 rows carry `untested` qualifiers in the Notes column (e.g. §1.1 row 3 — region SDK-default fallback path). Per the chosen Path A from PBI-1 AC10a, these stay Status=Implemented/Stubbed with the gap annotated in Notes rather than being downgraded. Behavioral coverage of these `untested` paths is a Phase 4.10 concern. (Origin: PBI-1 spec-deviation note, recorded in §5.)
- **/code-review deferred findings (cumulative scope `0a95eae..HEAD`):**
  - NIT — §3 Item 17 Tier-1 marked `Y` despite the row's own Notes acknowledging the cited test (`pkg/gsrserde-go/core/cache_test.go:TestCacheSize:60`) is a basic-functionality stand-in that does not assert the size-eviction contract. Deferred (not downgraded) because (a) the §3 NIT was raised and accepted by the PBI-3 impl-reviewer in round 1, and (b) downgrading the row mid-audit would change §3 counts already locked in by PBI-3. Behavioral remediation belongs to Phase 4.10. (Origin: `/code-review` against §3 row 17.)
  - NIT — §3 Item 26 Tier-1 cell cites only `avro_malformed_test.go` despite the row covering Avro + JSON + Protobuf; the JSON and Protobuf citations live in the Notes column instead. Deferred because the Notes column does carry the full citation set and the cell-style was approved by the PBI-3 impl-reviewer in round 1. (Origin: `/code-review` against §3 row 26 presentation.)

## 5. Methodology used

Audit conducted per `specs/phase-4.9-audit/spec.md` §Audit methodology, on branch `phase-4.9-audit` at cumulative HEAD covering PBI-1 (`82ea09c`) + PBI-2 (`f0ed7d3`) + PBI-3 (`4e89092`) + PBI-4 (this commit and its follow-up), all branched from base `0a95eae` on `golang-mrknox`.

Test-coverage decoupling: Path A — annotate untested behaviors in Notes — supersedes spec Step 2 line 148 per spec-reviewer MINOR #2 (see `specs/phase-4.9-audit/spec.md` and PBI-reviewer round 1 verdict). Under Path A, §1 rows whose Notes column carries an `untested` qualifier retain their Status verdict (`Implemented` or `Stubbed`) and surface the coverage gap in Notes rather than being downgraded.

Format-layer summary mapping: `Done` → `Implemented` column; `Still stub` → `Stubbed` column; `Other` → `Missing` column. (PBI-defined; spec is silent on this mapping.)

§5.3 test-coverage row presentation in the Summary counters table: option (b) — single row with `Tier-1: N1 Y / M1 N · Tier-2: N2 Y / M2 N` paired format in the `Implemented` cell, plus `Tier-1∧Tier-2 N: K` in the `Missing` cell to record items with both tiers absent. Tier counts are paired, not summed.
