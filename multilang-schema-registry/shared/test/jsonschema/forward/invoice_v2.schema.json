{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/gsr/forward/invoice_v2.json",
  "title": "Invoice",
  "description": "Forward-compatible evolution - added required 'status' field (old writers must supply it)",
  "type": "object",
  "additionalProperties": false,
  "required": ["invoiceId", "issued", "lineItems", "total", "status"],
  "properties": {
    "invoiceId": {
      "type": "string"
    },
    "issued": {
      "type": "string",
      "format": "date-time"
    },
    "lineItems": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["description", "quantity", "unitPrice"],
        "properties": {
          "description": { "type": "string" },
          "quantity": {
            "type": "integer",
            "minimum": 1
          },
          "unitPrice": {
            "type": "number",
            "multipleOf": 0.01
          }
        }
      }
    },
    "total": {
      "type": "number",
      "multipleOf": 0.01
    },
    "status": {
      "type": "string",
      "enum": ["draft", "issued", "paid", "void"]
    }
  }
}
